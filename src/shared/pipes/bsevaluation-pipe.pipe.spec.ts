import { BSEvaluationPipePipe } from './bsevaluation-pipe.pipe';

describe('BSEvaluationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BSEvaluationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
