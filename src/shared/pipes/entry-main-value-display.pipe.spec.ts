import { EntryMainValueDisplayPipe } from './entry-main-value-display.pipe';

describe('EntryMainValueDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryMainValueDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
