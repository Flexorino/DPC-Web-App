import { BSTendencyPipe } from './bstendency.pipe';

describe('BSTendencyPipe', () => {
  it('create an instance', () => {
    const pipe = new BSTendencyPipe();
    expect(pipe).toBeTruthy();
  });
});
